<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.UtopiaTTSRebirth">

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".activities.settings.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.UtopiaTTSRebirth" />

        <activity
            android:name=".activities.settings.CredentialsActivity"
            android:exported="false"
            android:theme="@style/Theme.UtopiaTTSRebirth" />

        <activity
            android:name=".activities.settings.InAppTtsSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.UtopiaTTSRebirth" />

        <activity
            android:name=".activities.innerTts.HistoryActivity"
            android:exported="false"
            android:theme="@style/Theme.UtopiaTTSRebirth" />

        <service
            android:name=".service.UtopiaTtsService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_TEXT_TO_SPEECH_SERVICE"
            android:exported="true">
            <intent-filter android:priority="100">
                <action android:name="android.intent.action.TTS_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.speech.tts"
                android:resource="@xml/tts_engine" />
        </service>

    </application>

</manifest>